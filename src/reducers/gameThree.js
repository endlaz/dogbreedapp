const initialState = {
    score: 0,
    streak: 0,
    totalQuestions: 0,
    level: 1
}

const reducer = (state = initialState, action = {}) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default reducer